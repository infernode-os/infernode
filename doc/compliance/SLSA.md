# Compliance Evidence — SLSA (Supply-chain Levels for Software Artifacts)

**Standard:** SLSA v1.0 (Build track).
**Roadmap row:** Supply chain & integrity — SLSA, Tier 0→1 ("At SLSA 3 today; push to L4").
**Tracking:** program epic [INFR-328]; L4 / SBOM work under [INFR-340].
**Artifact date:** 2026-06-22.
**Overall status:** **Met at Build Level 3.** Release artifacts are produced by a hosted,
isolated CI build that emits **non-forgeable provenance** and are **signed**. Build Level 4
(hermetic + reproducible) and SBOM are the tracked open push.

## 1. SLSA Build levels → evidence

| Level | Requirement | InferNode | Evidence |
|-------|-------------|-----------|----------|
| L1 | Provenance exists | ✅ build provenance attestations generated | `release.yml:1280-1288` (`actions/attest-build-provenance`) |
| L2 | Signed provenance, hosted build | ✅ provenance signed via Sigstore; GitHub-hosted runners | `release.yml:1242-1243` (`id-token`/`attestations`), `:1263-1278` (cosign) |
| **L3** | **Non-forgeable provenance; hardened/isolated build** | ✅ provenance bound to the build's OIDC identity (keyless Sigstore), generated by the platform, not the job; per-artifact `.sigstore` bundles + `SHA256SUMS` | `release.yml:1257-1288`; OpenSSF Scorecard `scorecard.yml` |
| L4 | Hermetic + reproducible build | ☐ open — push from L3 | INFR-340 (SBOM + L4) |

## 2. Supporting supply-chain integrity controls

- **Checksums:** `SHA256SUMS.txt` over every artifact (`release.yml:1257-1261`).
- **Keyless signing:** `cosign sign-blob` → `.sigstore` bundle per artifact, no long-lived
  signing key to steal (`release.yml:1263-1278`).
- **Provenance attestations:** tarball, DMG, zip (`release.yml:1280-1288`).
- **Pinned dependencies:** CI actions pinned to commit SHAs, not floating tags — closes the
  mutable-tag supply-chain vector.
- **Continuous posture scoring:** OpenSSF Scorecard (`.github/workflows/scorecard.yml`).
- **Artifact-content guard:** ring-fence CI fails the build if testing-only harness files leak
  into a release (`CLAUDE.md` "Ring-fence rule"; `release.yml`, `ci.yml`).
- **Bytecode integrity:** `verify-dis-paths` ensures shipped `.dis` matches source
  (`.github/workflows/verify-dis-paths.yml`; `tools/verify-dis-paths.sh`).

## 3. Open items (tracked, not blocking the L3 close)

| Item | Standard | Tracking |
|------|----------|----------|
| **SBOM** (SPDX) generated + validated in CI | roadmap "SBOM (SPDX/CycloneDX)" | ✅ `.github/workflows/sbom.yml` (syft → SPDX-JSON, validated, uploaded, on every PR/push) |
| **SBOM attached to releases** + provenance attestation | roadmap "SBOM" / SLSA L4 | ☐ release.yml wiring — INFR-340 |
| **in-toto** attestation beyond build-provenance | roadmap "in-toto" | INFR-340 |
| **Hermetic + reproducible** build (SLSA L4) | SLSA L4 | INFR-340 |

These are CI-config / documentation additions (no runtime code); any workflow change is
flagged for review.

**SBOM scope note (honest):** InferNode's core is first-party C and Limbo with no
package-manager manifests, so the generated SPDX SBOM principally captures the
third-party components that *do* carry external provenance (e.g. the Go modules under
`tools/godis`, Android/Gradle deps) plus detected binaries — which is the set an SBOM
exists to track for vulnerability management. The validation step fails CI if generation
yields zero packages, so an empty/broken SBOM is caught.

## 4. Disposition

**Met at SLSA Build L3.** The provenance + signing + checksum + pinning + scorecard evidence
above substantiates the roadmap's "at SLSA 3" claim. **SBOM generation is now exercised by
CI on every PR/push** (`sbom.yml`); the remaining SR push is attaching the SBOM to release
artifacts with an attestation (release.yml), plus hermetic/reproducible builds for L4 —
tracked under INFR-340.

## 5. References

- SLSA v1.0 (slsa.dev); OpenSSF Scorecard; Sigstore/cosign; SPDX / CycloneDX; in-toto.
- `.github/workflows/release.yml`, `sbom.yml`, `scorecard.yml`, `verify-dis-paths.yml`.
