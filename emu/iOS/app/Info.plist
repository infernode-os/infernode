<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  InferNode iOS app bundle metadata — hellaphone Phase 2b, B0.
  Minimal keys for a simulator (headless) build; no entitlements, no
  signing identity required for the simulator. Device/TestFlight (B3)
  will add entitlements and a real bundle id under a signing team.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>InferNode</string>
	<key>CFBundleIdentifier</key>
	<string>os.infernode.ios</string>
	<key>CFBundleName</key>
	<string>InferNode</string>
	<key>CFBundleDisplayName</key>
	<string>InferNode</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>0.1</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>MinimumOSVersion</key>
	<string>14.0</string>
	<!-- iOS 14+ blocks connections to local-network (LAN) addresses, e.g. a
	     remote InferNode at tcp!10.x!5640, unless the app declares this
	     purpose string; iOS then prompts for Local Network access on the
	     first such connection. Without it the dial+mount of /n/llm (and any
	     LAN 9P service) silently fails. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>InferNode connects to a remote InferNode on your local network to use its LLM and 9P services.</string>
	<!-- /phone/contacts surfaces the address book to Veltro so the agent
	     can resolve names ("text mom") to numbers. Read-only; the
	     contacts file is exposed through Inferno's 9P namespace under
	     /phone/contacts. Authorization status is checked on every read. -->
	<key>NSContactsUsageDescription</key>
	<string>InferNode reads your contacts so the Veltro agent can resolve names like "text mom" to phone numbers for SMS and dialling.</string>
	<!-- /phone/bio_* uses LocalAuthentication + Keychain biometryCurrentSet
	     to protect remote-LLM and secstore keys. iOS displays this string
	     under the Face ID prompt. -->
	<key>NSFaceIDUsageDescription</key>
	<string>InferNode uses Face ID to unlock signed keys (LLM mount, secstore) without prompting for a password every launch.</string>
	<key>UIDeviceFamily</key>
	<array>
		<integer>1</integer>
		<integer>2</integer>
	</array>
	<key>CFBundleSupportedPlatforms</key>
	<array>
		<string>iPhoneSimulator</string>
	</array>
	<key>DTPlatformName</key>
	<string>iphonesimulator</string>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
