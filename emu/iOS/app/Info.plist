<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  InferNode iOS app bundle metadata — hellaphone Phase 2b, B0.
  Minimal keys for a simulator (headless) build; no entitlements, no
  signing identity required for the simulator. Device/TestFlight (B3)
  will add entitlements and a real bundle id under a signing team.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>InferNode</string>
	<key>CFBundleIdentifier</key>
	<string>os.infernode.ios</string>
	<key>CFBundleName</key>
	<string>InferNode</string>
	<key>CFBundleDisplayName</key>
	<string>InferNode</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>0.1</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>MinimumOSVersion</key>
	<string>14.0</string>
	<key>UIDeviceFamily</key>
	<array>
		<integer>1</integer>
		<integer>2</integer>
	</array>
	<key>CFBundleSupportedPlatforms</key>
	<array>
		<string>iPhoneSimulator</string>
	</array>
	<key>DTPlatformName</key>
	<string>iphonesimulator</string>
	<key>UILaunchScreen</key>
	<dict/>
	<!-- Required for UIKit to deliver indirect-input events (mouse,
	     trackpad, simulator host clicks) to the app. Without it the
	     Simulator silently drops every host mouse click — SDL3 logs
	     `You need UIApplicationSupportsIndirectInputEvents in your
	     Info.plist for mouse support` and programmatic taps via
	     pyautogui / test harness never become SDL events. Touches on a
	     real device are unaffected, but in-sim iteration is blocked
	     without this. -->
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
