tool=exec
	description='Execute a shell command via sh.dis'
	authorities='spawns_proc execs_code reads_fs writes_fs'
	irreversible='spawns_proc writes_fs'
	notes='Force multiplier. Grants anything the spawned shell can reach within the agent namespace. Runtime nsconstruct keeps exec networkless unless a network-capable tool is the invoked capability. Scope limited by caps.shellcmds if set, otherwise unrestricted.'
