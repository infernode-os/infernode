tool=wallet
	description='Cryptocurrency and fiat payment operations via wallet9p'
	authorities='spends reads_fs'
	irreversible='spends'
	notes='Signing happens inside wallet9p, which holds keys via factotum; the agent never sees private keys or the raw sign oracle (agents get structured pay/authorize files only). wallet9p enforces budget caps and the trusted approval queue on every execution path at runtime. Declare caps.walletbudget to bound spend at the configuration level.'
