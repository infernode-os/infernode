rule=UNBOUNDED_SPEND
	severity=high
	require='spend_ungated'
	message='A spend authority (wallet/pay) is granted with no caps-level budget. Per-transaction wallet9p gating still applies at runtime, but the configuration declares no ceiling.'
	fix='declare caps.walletbudget to bound spend, or remove the spending tool'
